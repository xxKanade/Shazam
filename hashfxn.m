% hashfxn() calculates a location by taking in three parameters, the peak
% frequency, DT (IDK WHAT THIS IS), and SIZE(IDK WHAT THIS IS PLS DEFINE).
function hashme = hashfxn(pk, dt, size)
    % define this thing below
    hash = mod(round(), size)+1;

end